version: "3.3"
services:
    gateway:
        image: ${REGISTRY}/service-user-test-gateway:${IMAGE_TAG}
        restart: always
        volumes:
            - /etc/letsencrypt:/etc/letsencrypt:ro
            - /var/www/html:/var/www/html:ro
        ports:
            - "80:80"
            - "443:443"
        depends_on:
            - frontend
            - api

    frontend:
        image: ${REGISTRY}/service-user-test-frontend:${IMAGE_TAG}
        restart: always

    api:
        image: ${REGISTRY}/service-user-test-api:${IMAGE_TAG}
        restart: always
        depends_on:
            - api-php-fpm

    api-php-fpm:
        image: ${REGISTRY}/service-user-test-api-php-fpm:${IMAGE_TAG}
        restart: always

    api-php-cli:
        image: ${REGISTRY}/service-user-test-api-php-cli:${IMAGE_TAG}
